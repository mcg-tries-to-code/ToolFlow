{
  "schemaVersion": "toolflow.workflow/v1",
  "name": "health-downtrend-monitor",
  "description": "Run the bounded health downtrend monitor through the ToolFlow session lane.",
  "inputs": {
    "workspaceRoot": "/Users/jarvis/.openclaw/workspace",
    "scriptPath": "/Users/jarvis/.openclaw/workspace/scripts/check_health_downtrends.py",
    "statePath": "/Users/jarvis/.openclaw/workspace/data/health-monitor/downtrend-monitor-state.json",
    "sheetId": "1NtoXrJNZs4S6KDfVThis7Tsve10WrQzcMz4YZ0zKUqs",
    "sheetRange": "Sheet1!A:L",
    "target": "",
    "pythonBin": "python3"
  },
  "steps": [
    {
      "id": "health_downtrend_monitor",
      "action": "health_downtrend_monitor",
      "args": {
        "workspaceRoot": "${inputs.workspaceRoot}",
        "scriptPath": "${inputs.scriptPath}",
        "statePath": "${inputs.statePath}",
        "sheetId": "${inputs.sheetId}",
        "sheetRange": "${inputs.sheetRange}",
        "target": "${inputs.target}",
        "pythonBin": "${inputs.pythonBin}"
      }
    }
  ]
}
